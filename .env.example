# Example only. Replace the placeholder with this repository's absolute WSL2
# path when using a tool that loads .env files. The application does not load
# this file automatically.
LIGHTSAPP_DATA_DIR=/absolute/path/to/repository/.local-data
